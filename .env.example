# Mocksi Environment Configuration (FREE VERSION)
# Copy this file to .env and fill in your actual values

# Hugging Face API Configuration (FREE - no payment required!)
# Get your FREE token from: https://huggingface.co/settings/tokens
# Just create a free account and generate a token with 'read' access
HUGGINGFACE_API_TOKEN=#huggingface token replace this!!!!!!

# Temporal Configuration (usually don't need to change for local development)
TEMPORAL_HOST=temporal
TEMPORAL_PORT=7233
TEMPORAL_NAMESPACE=default

# FastAPI Configuration
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# React Frontend Configuration
REACT_APP_API_URL=http://localhost:8000

# Optional: OpenAI (if you want to use it instead of Hugging Face)
# OPENAI_API_KEY=your_openai_api_key_here